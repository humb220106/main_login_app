import 'package:flutter/material.dart';

// / for m26 navigation
class PageM6 extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text('My Course')),
      body: Center(child: Text('View My Course Details')),
    );
  }
}